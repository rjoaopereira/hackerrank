{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "target/debug/circular_array_notation",
            "cwd": "${workspaceRoot}"
        }
    ]
}